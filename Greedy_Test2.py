def solution(name):
    answer = 0
    nameQ = [name[i] for i in range(len(name))]
    print(nameQ)
    while nameQ:
        s = nameQ.pop(0)
        if s == 'A':
            pass
        else:
            # 알파뱃 맞추기
            pass
        name.rfind()

    return answer

# 실패 포기 안해 씨발
print(solution("ABCDAEA"))